function greeting(name,age){
    console.log(`Welcome, ${name}. you are ${age} old. so, you are eligible for vote..`);
}
greeting("Ponraj",20);
greeting("Narendaren",21);

//A parameter is a variable declared in the function definition that acts as a placeholder. An argument is the actual value passed to the function when the function is called.