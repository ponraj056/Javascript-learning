function one(a,b){
    return a+b;
}
console.log(one(20,30));
// in this context a=20 and b=30. so the output is 50. this is called execution context. 
// it is created when the function is invoked.

